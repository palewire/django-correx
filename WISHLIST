* Package everything for distutils in setup.py
* Figure out if there's a standardized "best practice" for packaging the admin override template
* Improve the jQuery in the admin override template so it approaches something worth using
* Considering adding a PickledObjectField to the ChangeType model or another method that would allow the denormalized
storage of user, site, app, model and object totals